FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY mcp_server/ ./mcp_server/

EXPOSE 8765

CMD ["python", "mcp_server/server.py", "--http", "--port", "8765"]
